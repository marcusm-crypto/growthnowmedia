# Testimonial videos (~1.1GB) ship with the deploy so they play on the live
# site. They are self-hosted from public/testimonials/. If deploy size or
# bandwidth becomes a problem, move them to a CDN/Vimeo and point the
# videoUrl fields in src/data/testimonials.ts at the external URLs instead.

# Never upload these (Vercel rebuilds/installs in the cloud)
node_modules/
dist/
.astro/
.vercel/
.git/
.DS_Store
